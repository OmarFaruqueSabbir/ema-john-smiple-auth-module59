/**
 * 1. create a new project a console.firebase.google.com
 * 2. npm install firebase.
 * 3. create firebas_init.js and import getAuth to export auth in firebase.init.js
 * 4. firebase settings > Authentication > enabke Email & Password authentication.
 * 5.
 */

/**
 * hosting steps:
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
 */